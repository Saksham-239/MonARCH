# =============================================================================
# PI-MDCNet v4 (SLAC) — Python 3.12, CUDA 12.x (RTX 2050)
# =============================================================================

# --- Deep Learning ---
torch>=2.3.0
torchvision>=0.18.0

# --- Scientific Computing ---
numpy>=1.26.0,<2.0
scipy>=1.12.0
pandas>=2.2.0
h5py>=3.10.0
hdf5storage>=0.1.19        # NASA PCoE .mat v7.3 (HDF5) loading

# --- Classical ML Baselines ---
scikit-learn>=1.4.0
xgboost>=2.0.0

# --- Hyperparameter Tuning ---
optuna>=3.5.0

# --- Visualization ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- ONNX Export & Inference ---
onnx>=1.15.0
onnxruntime>=1.17.0

# --- TFLite Conversion Path (embedded track) ---
# onnx-tf or onnx2tf for ONNX→TF conversion
# ai-edge-litert for INT8 quantization (Python 3.12 compatible)
# Deferred install — these have heavy transitive deps; install when Phase 5 starts

# --- Utilities ---
tqdm>=4.66.0
